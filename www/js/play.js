document.addEventListener('DOMContentLoaded',function(){appPlay.initialize()});
var appPlay = {
	initialize:function(){
		console.log('appPlay');
		//history.pushState(null, "", location.href.split("?")[0]);
	}
};